spring:
  application:
    name: selman01
  datasource:
    url: jdbc:mysql://localhost:3306/selman
    username: root
    password: hSelMan%231?!
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    database-platform: org.hibernate.dialect.MySQL8Dialect
    hibernate:
      ddl-auto: update
    show-sql: true

